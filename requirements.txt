# account-nuker v1.1.0 — requirements
# Install: pip install -r requirements.txt
# Then:    playwright install chromium --with-deps

# Core
imap-tools>=1.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# TUI
rich>=13.0.0
textual>=0.40.0
click>=8.1.0

# Browser automation (headful Playwright)
playwright>=1.40.0
# After pip install: run   playwright install chromium --with-deps
